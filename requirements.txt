# Data-Speaks — Python dependencies
# Target Python: >=3.10 (MNE 1.6+ requires 3.9+; type-hint syntax in
# pipeline_a.py uses `X | None`, which needs 3.10+)

# ---------------- Core (everyone needs these) ----------------
mne>=1.6,<2.0          # EEG I/O (EDF/FIF), filtering, ICA, resampling, QC
numpy>=1.24            # array backbone; pin floor for typing/performance
scipy>=1.10            # signal processing (underpins MNE + features)
pandas>=2.0            # metadata.csv / window_metadata.csv handling
scikit-learn>=1.3      # stages 04-06 (PCA, clustering, classification)
matplotlib>=3.7        # figures -> results/figures/
seaborn>=0.13          # publication-style plots on top of matplotlib
joblib>=1.3            # parallel filtering (n_jobs), model persistence

# ---------------- Stage 0 & dataset audit ----------------
pyedflib>=0.1.38       # fallback EDF reader (integrity checks); MNE EDF
                       # reading goes through this under the hood anyway
tqdm>=4.66             # progress bars for full-dataset audits (hours-long)

# ---------------- Pipeline-specific (install all; each owner uses theirs) ----------------
PyWavelets>=1.5        # Pipeline B: wavelet denoising
meegkit>=0.1.6         # Pipeline C: ASR (artifact subspace reconstruction)
autoreject>=0.4        # optional Pipeline C/D: automated bad-channel/window
                       # rejection — only add if actually benchmarked

# ---------------- Notebooks & reporting ----------------
jupyter>=1.0           # notebooks/01-06
ipykernel>=6.25        # lets notebooks run inside the project venv
pyarrow>=14.0          # optional: fast parquet caches for window metadata

# ---------------- Dev / evaluation hygiene ----------------
pytest>=7.4            # unit tests for substeps + shared evaluator