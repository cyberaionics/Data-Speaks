# Data-Speaks — EEG Seizure Detection (Pipeline A + Math Section)

Solo reproduction + extension of Shoeb & Guttag (2010) on the
CHB-MIT Scalp EEG Database.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt